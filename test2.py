#!/usr/bin/env python

#will use sys for command-line arguements
import sys

#command-line arguments should be numbers
nums = []
for num in sys.argv:
	nums.append(int(num))

def sum(liftOfInts)
"""This function will calculate the sum of intergers in a list. """
	total = 0
	for myInt in listOfInts:
		total = total + myInt
print(sum(nums))
